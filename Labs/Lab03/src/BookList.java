// Q3

public class BookList<L> extends ObjectList<L> {                            // extends to use inheritance, BookList is a subclass of ObjectList

    // Getters and Setters
    public void getBookList() {
        getObjectArray();
    }
}